name: "Hello World"
on: 
  pull_request:
     branches: 
       - main
  push:
jobs:
  Hello-world:
    strategy:
      matrix:
        name: [ubuntu-latest]
    runs-on: ${{ matrix.name }}
    steps:
      - name: hello world
        env:
          name: ${{secrets.MY_NAME}}
        run: |
         echo "Hello Prem"
         echo "hello jeeva"
         echo "hello ${{ vars.MY_NAME }}"
         echo $name
    
